{
    "name": "<%=appname%>",
    "version": "0.0.1",

    "scripts": {
        "eslint": "eslint .",
        "mocha": " istanbul cover --root ./generators  --include-all-sources --dir ./coverage/mocha --report text --report text-summary --report lcov --print none  _mocha -- test/mocha/**/*.spec.js -R spec",
        "test": "npm run eslint && npm run mocha"
    }
}